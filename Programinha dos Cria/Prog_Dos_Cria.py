from arquivo import *
from commands import *
from binance.client import Client


# Log-in with websocket
"""kbm = BinanceSocketManager(kClient)
bm = BinanceSocketManager(cClient)
getOrders(kClient, k_Asset, k_Price, k_Side)
"""



# (End) Refazer o bot da maneira antiga, melhor escrito, multiplos bots pra verificação de dados e um main bot simples para execução.
# 0 Precisa verificar se a quantidade for maior que o mínimo permitido
# 1 Cancelar ordem do user quando server cancelar
# 2 Fazer teste de exceções e erros
#
#
#

# ? Conectar a conta pelo websocket (Provavelmente não)
